# しごと図鑑 — 完成の基準

測れない基準は書かない。実測値は `npm run verify` と
`src/data/generated/graph-health.json` から転記する。

最終計測日: 2026-08-06

## 0. これは何か

世の中にある職業そのものを集めた図鑑。求人サイトではない。

人は自分の生活で見た仕事しか思いつけない。求人サイトは「探している職種名」を
知っている前提で作られているので、名前を知らない仕事には一生行き当たらない。
このサイトは職業どうしをタグでつなぎ、**知っている仕事から辿ると知らない仕事に出る**
ように作る。

### やらないこと（非目標）

- 求人情報・給与・募集の掲載（求人票ではない）
- **全エッジを常時描いた**グローバルマップ（毛玉になり、個々が読めなくなる）
  - 当初は「全体マップそのもの」を非目標にしていたが、毛玉の正体は
    ノード数ではなく常時描かれる大量の線だった。
    線をフォーカス中の1件からの6本だけに絞れば成立するため、
    トップページに全体マップを置いている（後述）。
- 職業の網羅（日本標準職業分類を端から埋めることは目指さない）
- ユーザー投稿・コメント・ログイン・パーソナライズ
- 力学シミュレーション（d3-force 等）によるグラフ描画

### 収録方針

既知の職業を少数だけ入口として置き、そこから2〜3歩で未知の珍しい仕事に出る構成にする。
「エンジニア」「営業」のような誰でも知っている職業を並べることは目的ではない。

## 1. v1: 成立 —「50件で芋づる式に辿れる」

- [x] 職業が50件以上ある（実測 **53件**）
- [x] 全件が必須フィールドを持つ（実測 53/53、`data:validate` の E6 が0件）
- [x] 全件がタグ6件以上・4軸以上（実測 53/53、E11・E12 が0件）
- [x] タグ総数が80件以上（実測 **132件**）
- [x] 1件にしか使われないタグが15%以下（実測 **10%**、13/132）
- [x] 入口職業（familiarity>=4）が8件以上（実測 **18件**）
- [x] 入口集合から全職業に到達できる（実測 **到達率 100.0%**）
- [x] どこからも辿り着けない職業が0件（実測 **0件**）
- [x] 全ての入口から3ホップ以内に rarity>=4 へ到達（実測 **平均 1 ホップ**、
      未到達の入口 0件）
- [x] `npm run data:validate` の ERROR が0（実測 ERROR 0 / WARN 8）
- [x] `npm run lint` が通る（実測 エラー・警告とも0）
- [x] `next build` が成功する（実測 **201ページ**生成）
- [x] `/jobs/[slug]` に図と関連職業リストの両方がある
- [x] 図が初期HTMLに含まれている（JSを待たずに絵が出る。実測 `ego-node` 14件が
      静的HTMLに存在）
- [x] 図を消してもリンクだけで3ホップ辿れる（SVGを除去した状態で実測: 
      鉄道運転士 → 臨床工学技士 → 旅客機パイロット）
- [x] 375px で横スクロールが発生しない（9種類のページで実測 scrollWidth = 375）
- [x] モバイルで図が破綻しない（外リング6件を非表示にし、内リング8件のみ表示。
      関連職業リストには14件すべてが残る）
- [x] ブラウザのコンソールエラーが0件
- [x] ライト・ダーク両方で本文とファセット7色が 4.5:1 以上
      （実測 最小 4.52、ダーク時 最小 6.49）
- [x] 軸で絞ると隣人が変わる（パティシエ: 全体14件 → 感覚11件 → 業界4件）
- [x] 通ってきた道が表示され、3歩以上で出発点からの距離が出る

### 全体マップ（トップページ）

- [x] 全53件が1枚に載っている（実測 53ノード）
- [x] **ページ遷移が起きない**（選択・フォーカス移動を通じて URL が変わらないことを実測）
- [x] 何も選んでいない状態で線が0本（毛玉にならないことの実測）
- [x] 選択すると線は6本だけ、無関係な46件は沈む
- [x] ノードが重ならない（実測 最小中心間距離 24.1px / ノード径 22.5px）
- [x] 当たり判定がSVGの丸と一致（実測 ずれ 0px）
- [x] フォーカス移動だけで探索できる（4件を順に辿り、パネル・線・強調が追随）
- [x] パネル内の隣人を選んでもページ遷移せず、地図上のフォーカスだけが移る
- [x] 375px でページが横スクロールしない（地図だけが内部で横スクロールする）
- [x] 375px でタップ領域が重ならない（実測 間隔 21.9px / 径 20.5px）
- [x] コンソールエラー0件

### 探索の実測（入口5件・毎回1番目の隣人を辿る）

| 入口 | 1歩目 | 2歩目 |
|---|---|---|
| パティシエ（意識しないと会わない） | 筆職人（数百人規模） | 真珠養殖の核入れ職人（数十人規模） |
| 鉄道運転士（意識しないと会わない） | 航空管制官（数百人規模） | 旅客機パイロット |
| 動物園飼育員 | 遺品整理士（数百人規模） | — |
| 気象予報士 | 航空管制官（数百人規模） | 旅客機パイロット |
| ソムリエ | 葬祭ディレクター | エンバーマー（数十人規模） |

いずれも2歩以内に rarity>=4 へ到達している。

## 2. v2: 公開できる

- [ ] 職業150件・タグ150件
- [ ] Cloudflare Pages へデプロイし、URLを確定して `metadataBase` を更新
      （現在は `src/app/layout.tsx` の `SITE_URL` が仮の値。
      `sitemap.ts`・`robots.ts` はここを参照しているので1か所の変更で揃う）
- [ ] 全ページに OGP 画像
- [ ] サーチコンソール登録・GA4 設置
- [ ] 図のキーボード操作（←→で巡回、Enterで移動、`aria-live` で読み上げ）
- [ ] `/explore`（図を全画面にした探索モード）と、通ってきた道のまとめ
- [ ] 全件に出典を1件以上（現在 WARN W8 で未記載を検出できる）
- [ ] モバイルの2段階タップ（1回目で下部シート、2回目で移動）

## 3. v3: 差別化

- [ ] 職業500件、タグの親子関係を30組以上
- [ ] 手書きの型付きエッジを80件以上（`confusable`・`front-and-back` 中心）
- [ ] 「今日の職業」（日付ハッシュで決定論的に選ぶ。静的でも実装できる）
- [ ] 通ってきた道を画像で共有
- [ ] `nameEn` を全件に入れ、英語対応の下地を作る

## 4. データの扱いに関する原則

1. **タグは自由文字列にしない。** すべて `src/data/taxonomy/tags/*.json` に
   定義された `Tag.id` への参照であり、未定義の参照は `data:validate` が落とす。
   50件が2000件になっても表記ゆれが混入しないのは、この一点による。
2. **職業どうしの線は手書きしない。** 共有タグから自動導出する。
   手書きは「タグでは絶対に出ない関係」だけに限り、1職業あたり3件まで。
3. **生成物 (`src/data/generated/`) はコミットする。** タグを1つ足したときに
   隣人がどう変わったかが `git diff` で見えることが、キュレーション品質の
   管理手段として一番価値がある。古くなっていないかは CI が
   `git diff --exit-code` で検出する。
4. **タグは削除せず `deprecated` にする。** URLを死なせないため。
   参照が残っていれば E7 が拾う。
5. **`rarity` と `familiarity` は独立に付ける。** 基準は
   `src/data/taxonomy/rubric.json`。個々の値の正しさは検査できないが、
   分布のインフレは W9 が検出する。

## 5. 実装の現在地

| 領域 | 状態 |
|---|---|
| データモデルと正規化 | ✅ 型・命名規則・統制語彙・バリデータ（E1〜E18 / W1〜W11） |
| 隣人の自動導出 | ✅ IDF × 軸重み × 意外性ブースト × MMR × 相互性保証 |
| 職業ページ | ✅ 図・関連職業リスト・軸別タグ・出典 |
| エゴネットワーク図 | ✅ 決定論的極座標・セクター化・依存ライブラリなし |
| 全体マップ（トップ） | ✅ 角度=業界／半径=既知度。線はフォーカス中の6本のみ。ページ遷移なし |
| 軸での絞り込み | ✅ URL同期あり |
| 通ってきた道 | ✅ sessionStorage |
| 周辺ページ | ✅ トップ・一覧・軸・タグ・統計・about |
| CI | ✅ build + lint + validate + 生成物のドリフト検出 |
| デプロイ | ✗ 未実施（v2） |
| 図のキーボード操作 | ◐ リンクとしては辿れるが、矢印キー巡回は未実装（v2） |
